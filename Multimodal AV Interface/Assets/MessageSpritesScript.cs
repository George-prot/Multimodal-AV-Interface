using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MessageSpritesScript : MonoBehaviour
{
    public GameObject inboxSpriteOn;
    public GameObject outboxSpriteOn;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
