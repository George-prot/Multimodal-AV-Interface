using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PhoneSpritesScript : MonoBehaviour
{
    public GameObject recentCallsSpriteOn;
    public GameObject contactsSpriteOn;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
