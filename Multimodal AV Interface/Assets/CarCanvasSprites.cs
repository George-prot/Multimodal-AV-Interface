using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CarCanvasSprites : MonoBehaviour
{
    public GameObject enginePresetSprite;
    public GameObject tirePressureSprite;
    public GameObject maintenanceSprite;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
